import { PageStub } from "@/components/app/page-stub";
export default function P() {
  return (
    <PageStub
      chip="Content moderation"
      title="Reported content"
      subtitle="Review reports for chat messages and posts."
    />
  );
}
