import { PageStub } from "@/components/app/page-stub";
export default function P(){return <PageStub chip="Reward management" title="Reward rules & history" subtitle="Daily reward amounts, streak bonuses, mission rewards, reward logs."/>;}
